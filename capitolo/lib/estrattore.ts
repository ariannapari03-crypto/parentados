// I prompt di estrazione, versionati, vivono qui (compito ④). Ogni estrazione
// registra `prompt_versione` così da poter confrontare la qualità nel tempo.
// Distinti: uno per le regole del corso, uno per il calendario del dipartimento.
// La chiave API si legge solo da variabile d'ambiente, mai dal browser. [inv. 8]

export const VERSIONE_PROMPT = '0';
